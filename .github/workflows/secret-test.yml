name: Secret Test
on:
  workflow_dispatch:

jobs:
  test-secrets:
    runs-on: self-hosted
    steps:
      - name: Simple step (no secret output)
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "Secrets are loaded (not printed)."